/**
 * Milestone 5 — format a report for the CLI.
 * Pure string-building: testable offline, no I/O.
 */
import type { PokemonSummary } from "./transform";

/**
 * Milestone 5 — a readable multi-line report. Must include each Pokémon's
 * name, its types, and its stats. Layout is yours — the tests only check
 * content, not whitespace.
 */
export const formatReport = (summaries: readonly PokemonSummary[]): string => {
  throw new Error("TODO: milestone 5");
};
